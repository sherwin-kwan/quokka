<section>
  <section class="question-header">
    <p>Question #<%= question.number %></p>
    <% if ((() => {
      for (const questionOption of question.options) {
            if (questionOption.is_correct && questionOption.user_selected) {
              return true;
            }
          }
          return false;
        })()) { %>
      <p class="result-word-correct">CORRECT</p>
    <% } else{ %>
      <p class="result-word-incorrect">INCORRECT</p>
      <% } %>
  </section>
  <p><%= question.content %></p>

  <% for (const option of question.options) { %>
    <%- include('../partials/_question-answer.ejs',
      {
        questionIndex : question.number,
        option: {
          index: 0,
          label: option.answer_text,
          clickable: question.clickable,
          showCorrect: question.showCorrect,
          correct: option.is_correct,
          selected: option.user_selected
        }
      }
    ); %>
  <% }; %>
</section>
