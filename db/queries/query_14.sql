--This query selects a user's fname and lname given their user ID.

SELECT fname, lname
FROM users
WHERE id = 1 --This is a placeholder; it should be replaced with the id of the user that is logged in.
